# 2026-04-26 Smoke20 主线跑通报告

## 1. 本次执行范围

按既定顺序实际运行了 4 条主线 `smoke20` 实验：

1. `single-agent` main-table same-context
2. `debate-vs-vote` controlled same-context
3. `SPARC` same-context end-to-end
4. `HotpotQA split-context` communication-necessary

为避免 DashScope `HTTP 429 limit_requests`，本轮将以下配置调为保守速率：

- `configs/single_agent/experiments/main_table_same_context.toml`
- `configs/multi_agent/experiments/debate_vs_vote_main_table.toml`
- `configs/sparc/experiments/sparc_main_table_same_context.toml`
- `configs/comm_necessary/experiments/hotpotqa_split500_main.toml`

统一调整为：

- `max_concurrent_requests = 2`
- `requests_per_minute_limit = 20`

## 2. 运行产物

### 2.1 Single-Agent

- 运行目录：
  `local/runs/single_agent/20260426T064027Z-main-table-same-context-smoke20`
- 校验结论：`passed = true`
- `request_failures_total = 0`
- 备注：存在少量 `schema_fail`，但整体输出成功率仍为 `0.9972`

主结果摘要：

| Dataset | CoT | SC5 | MV3 |
| --- | ---: | ---: | ---: |
| `math500` | 0.25 | 0.35 | 0.35 |
| `hotpotqa` | 0.65 | 0.65 | 0.65 |
| `mmlu_pro` | 0.60 | 0.60 | 0.50 |
| `gpqa_diamond` | 0.30 | 0.45 | 0.45 |

判断：

- 结果整体**符合预期**
- 新 benchmark 的 loader / prompt / scoring 链路是通的
- `GPQA Diamond` 上 `SC5/MV3 > CoT`
- `MATH500` 分数低但在合理区间，没有出现明显评分错乱
- `MMLU-Pro` 与 `HotpotQA` 的量级也合理，没有“全部 0 分”或“异常满分”

### 2.2 Debate vs Vote Controlled

- 运行目录：
  `local/runs/multi_agent/20260426T072843Z-debate-vs-vote-main-table-smoke20-dashscope-qwen-turbo-1101`
- 校验结论：`passed = false`
- 失败原因：`schema_failures = 8`
- `request_failures = 0`
- 配对报告已生成：
  `paired_debate_vs_vote.json`
  `debate_vs_vote_report.md`

配对结论（同一批初始候选上，initial vote vs debate vote）：

- `gpqa_diamond`: `0.50 -> 0.50`，`delta = 0.00`
- `hotpotqa`: `0.50 -> 0.55`，`delta = +0.05`
- `math500`: `0.20 -> 0.15`，`delta = -0.05`
- `mmlu_pro`: `0.50 -> 0.50`，`delta = 0.00`

等预算控制 summary（`mad_3a_r1` vs `mv_6/sc_6`）显示：

- `gpqa_diamond`: `MAD = MV6 = SC6 = 0.50`
- `hotpotqa`: `MAD = 0.55 < MV6 = 0.60`
- `math500`: `MAD = 0.15 < MV6 = 0.25`
- `mmlu_pro`: `MAD = MV6 = SC6 = 0.50`

判断：

- 结果**基本符合预期**
- 核心结论是：**debate 没有稳定带来独立增益**
- 这与论文要对齐的 “Debate skepticism / Debate-or-Vote” 方向是一致的
- 但这条 run 仍有 8 个 `schema_fail`，都集中在 `gpqa_diamond`
- 因此它满足“无 request failure”，但**不满足“全链路格式零失败”**

### 2.3 SPARC Same-Context

- 运行目录：
  `local/runs/sparc/sparc_v1/20260426T075402Z-sparc_main_table_same_context-smoke20-dashscope-qwen-turbo-1101`
- 校验结论：`passed = false`
- 失败原因：`schema_failures_total = 5`
- `request_failures_total = 0`
- 额外检查：
  - `shared_stage_a_hash_check = passed`
  - `skipped_audit_zero_token_check = passed`
  - `local_audit_scope_check = passed`

overall 主结果：

| Method | Accuracy | Total Tokens | Comm Tokens | Audit Tokens |
| --- | ---: | ---: | ---: | ---: |
| `mv_3` | 0.3750 | 2262.55 | 0.00 | 0.00 |
| `always_communicate` | 0.3875 | 4974.23 | 2559.40 | 152.28 |
| `hybrid_trigger_baseline` | 0.3750 | 3009.81 | 747.26 | 0.00 |
| `final_round_vote_baseline` | 0.3750 | 4821.95 | 2559.40 | 0.00 |
| `sparc_v1` | 0.3875 | 3157.08 | 747.26 | 147.26 |

关键分数据集观察：

- `gpqa_diamond`: `mv_3 = 0.40`, `sparc_v1 = 0.45`
- `hotpotqa`: `mv_3 = 0.50`, `sparc_v1 = 0.60`
- `math500`: `mv_3 = 0.15`, `sparc_v1 = 0.10`
- `mmlu_pro`: `mv_3 = 0.45`, `sparc_v1 = 0.40`

判断：

- 结果**部分符合预期**
- 正向部分：
  - `SPARC` overall 与 `always_communicate` 持平
  - 但 `SPARC` 的总 token 明显更低
  - 相比 `final_round_vote_baseline`，`SPARC` overall 多 1 题正确，同时大幅减 token
  - `HotpotQA` 和 `GPQA Diamond` 上方向是好的
- 风险部分：
  - `math500` 和 `mmlu_pro` 上出现负迁移
  - `audit_turns` 的 5 个 `schema_fail` 全在 `math500`
- 因此当前最稳的表述不是“SPARC 全面赢”，而是：
  **SPARC 在部分任务上实现了更好的 accuracy-cost tradeoff，但数学与 MCQ 审计稳定性仍需继续打磨**

### 2.4 HotpotQA Split-Context

- 运行目录：
  `local/runs/comm_necessary/hotpotqa_split500_main/smoke20/20260426T080346Z-hotpotqa_split500_main-smoke20-dashscope-qwen-turbo-1101`
- 校验结论：`passed = true`
- `request_failures_total = 0`
- `schema_failures_total = 0`
- 关键约束全部通过：
  - `paired_design_check`
  - `context_leak_check`
  - `shard_union_check`
  - `packet_cap_check`
  - `hotpot_prediction_files_check`
  - `rate_limit_check`

主结果：

| Method | Ans EM | Sup F1 | Joint F1 | Comm Tokens |
| --- | ---: | ---: | ---: | ---: |
| `full_context_single` | 0.55 | 0.4917 | 0.3235 | 0.0 |
| `split_no_comm_mv3` | 0.25 | 0.3275 | 0.1726 | 0.0 |
| `answer_only_exchange` | 0.30 | 0.4401 | 0.1960 | 85.2 |
| `evidence_exchange` | 0.40 | 0.3992 | 0.2383 | 350.2 |
| `full_packet_exchange` | 0.40 | 0.3906 | 0.2560 | 602.5 |

关键比较：

- `evidence_exchange - split_no_comm_mv3`
  - `Ans EM +0.15`
  - `Joint F1 +0.0656`
- `full_packet_exchange - answer_only_exchange`
  - `Ans EM +0.10`
  - `Joint F1 +0.06`
  - 但额外通信成本非常高

判断：

- 结果**非常符合预期**
- 它清楚证明了：
  - 在 split-context setting 下，通信确实是有价值的
  - 但粗放 full-packet 并不显著优于更节制的 evidence exchange
- 这是目前四条实验里**最干净、最支持论文主张**的一条

## 3. 总结：是否达成“跑通且无请求失败”

### 已达成

- 四条主线都已成功运行并产出完整 run 目录
- 四条主线最终采用的有效 run **全部没有 request failure**
- `single-agent` 与 `split-context HotpotQA` 的正式校验都通过

### 未完全达成

- `debate-vs-vote` 仍有 `8` 个 `schema_fail`
  - 集中在 `gpqa_diamond`
- `SPARC` 仍有 `5` 个 `schema_fail`
  - 集中在 `math500` 的 audit turn

因此，严格来说，本次交付达成的是：

> **网络请求稳定性问题已解决，但多智能体链路在部分数据集上的结构化输出稳定性仍未完全清零。**

## 4. 对论文方向的结论判断

当前 smoke20 最值得保留的三条研究信号是：

1. **同预算下，debate 本身没有稳定独立增益**
   - `Debate-vs-Vote` 结果总体支持这条反思线

2. **通信必要性在 split-context HotpotQA 上被清楚验证**
   - `evidence_exchange` 明显优于 `split_no_comm_mv3`

3. **SPARC 的价值更像“更优的 accuracy-cost tradeoff”，而不是“全任务纯精度碾压”**
   - `overall` 持平 `always_communicate`
   - 但 token 更低
   - `HotpotQA` 上最有希望

## 5. 下一步建议

如果目标是尽快推进到可写论文的下一轮，我建议顺序固定为：

1. 先修 `gpqa_diamond` 上的 `multi-agent schema_fail`
   - 重点看 `multi_agent_controlled_json` 的输出约束，减少 reasoning 字段漂移

2. 再修 `math500` 上的 `SPARC audit` 稳定性
   - 当前负迁移和 `audit_turn` schema fail 是连在一起的

3. 然后只扩两条 `pilot100`
   - `debate-vs-vote-main-table`
   - `hotpotqa_split500_main`

4. 暂时不要直接扩 `SPARC main`
   - 先把 `math500/mmlu_pro` 的负迁移查清楚

## 6. 本轮推荐作为“当前最佳证据”的 run

- 最稳的基线 run：
  `local/runs/single_agent/20260426T064027Z-main-table-same-context-smoke20`

- 最稳的 communication-necessary run：
  `local/runs/comm_necessary/hotpotqa_split500_main/smoke20/20260426T080346Z-hotpotqa_split500_main-smoke20-dashscope-qwen-turbo-1101`

- 最值得继续打磨的方法 run：
  `local/runs/sparc/sparc_v1/20260426T075402Z-sparc_main_table_same_context-smoke20-dashscope-qwen-turbo-1101`
