# Frozen router artifacts

`rcta_v1.json` is generated only after both Qwen-Flash and MiMo-v2.5 `count300_seed42` runs complete and pass the pre-registered cross-fit gate. It is intentionally absent before fitting.
