# cred_v

CRED-V is the verifier-centric mainline after the CRED-MAD v5 shrink. It keeps
the same benchmark/control set for paired comparison and moves future SOTA work
toward verifier routing, tool checks, heterogeneous models, and adaptive compute.

