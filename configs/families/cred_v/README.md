# cred_v

CRED-V is the verifier-centric mainline after the CRED-MAD shrink. It keeps the
same benchmark/control set for paired comparison, uses SC-aligned free-text CoT
candidate generation, and moves future SOTA work toward verifier routing, tool
checks, heterogeneous models, and adaptive compute.
