# cred_v

CRED-V is the adaptive candidate-search mainline after the CRED-MAD shrink. It
keeps the same benchmark/control set for paired comparison, uses structured
Stage A role contracts, and tests whether selective `mimo-v2.5-pro` candidate
expansion plus verifiable aggregation can beat `cred_v_vote_5` without allowing
single-verifier promotion.
